{
	"name": "@character-builder/fastify-server",
	"license": "MIT",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "rimraf dist && tsup src/index.ts --format esm",
		"start": "node dist/index.js",
		"dev": "rimraf dist && tsup src/index.ts --format esm --watch & nodemon dist/index.js --watch"
	},
	"authors": [
		"thebadams",
		"Brian P. Adams"
	],
	"devDependencies": {
		"nodemon": "^3.0.1",
		"rimraf": "^5.0.1",
		"ts-node": "^10.9.1",
		"ts-node-dev": "^2.0.0",
		"tsup": "^7.2.0",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"@trpc/server": "^10.37.1",
		"fastify": "^4.21.0",
		"zod": "^3.21.4"
	}
}
